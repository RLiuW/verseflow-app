name: siemb
description: Flutter application for college final work.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"  # Atualizado para compatibilidade com android_intent_plus 6.0.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Firebase - Versões modernas (janeiro 2026)
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  # firebase_options será gerado automaticamente pelo comando 'flutterfire configure'

  # State Management
  scoped_model: ^2.0.0
  # Nota: scoped_model está descontinuado. Considere migrar para Provider ou Riverpod no futuro.
  # Alternativas modernas: provider: ^6.1.2 ou flutter_riverpod: ^2.5.1

  # YouTube Player
  youtube_player_flutter: ^9.1.3

  # URL Launcher
  url_launcher: ^6.3.2

  # Android Intent - Versão null-safe e moderna
  android_intent_plus: ^6.0.0
  # Migrado de android_intent (legado) para android_intent_plus (null-safe, Dart 3.1.0+)

  # UI Components
  scrollable_positioned_list: ^0.3.4
  # flutter_search_bar removido - usando SearchDelegate nativo do Flutter (null-safe)

  # Local Storage
  sqflite: ^2.3.3+2
  shared_preferences: ^2.3.0

  # Utilities
  fluttertoast: ^9.0.0
  intl: ^0.20.2
  flutter_phoenix: ^1.1.0

  # UI - Material 3 & typography
  google_fonts: ^6.2.1

  path: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - images/
    - bible/
    - assets/icon/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

# Launcher icon (VerseFlow) - run: dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/verseflow_icon.png"
  adaptive_icon_background: "#121212"
  adaptive_icon_foreground: "assets/icon/verseflow_icon.png"
